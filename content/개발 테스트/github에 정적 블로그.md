1. 옵시디언을 github에 올리는 법을 정리한다 
2. git과 node.js, npx를 다운로드 한다 
3. cmd 또는 powershell로 git init을 옵시디언 폴더 내로 들어가서 한다 
4. npx create-quartz@latest을 설치한다 
	-> Vault를 읽어서 웹사이트 구조로 변환을 시켜준다 
5. npx 버전이 높지 않으면 다음 순서로 다운로드하기
	1> git clone https://github.com/jackyzha0/quartz.git 
	2> cd quartz -> npx quartz create -> quartz 폴더 내 content 폴더 만들기 
	3> Copy an existing folder -> publish가 있는 폴더 경로 입력 -> 
	4> Treat links as shortest path (default) : 내부 링크를 Quartz가 자연스럽게 해석하는 기본값 -> 
	5> 
6. publish 폴더를 옵시디언 폴더 내에 만들어서 공개할 파일만 따로 공개하기 
7. 